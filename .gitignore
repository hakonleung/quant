# Node / pnpm
node_modules/
.pnpm-store/
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo

# Python / uv
__pycache__/
*.pyc
.venv/
.uv-cache/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/

# Generated cross-process artifacts
packages/shared/generated/
services/py/quant_core/generated/

# Env / secrets
.env
.env.local
.env.*.local
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
.AppleDouble

# Watch state (W-0): tasks.json is mutable runtime state. universe_{hk,us}.json
# under data/watch IS source-tracked — fresh clones get the lookup tables
# without an akshare round-trip.
apps/api/data/

# Per-user data dirs (post auth multi-user split). data/users/{uid}/...
# holds personal ledger / watch / sys-cfg / AI cache; data/users/_meta/users.json
# is the user registry. All user state, never source-tracked.
data/users/

data/all_users.parquet
data/public_sectors.parquet
data/.legacy

# Test output
coverage/
playwright-report/
test-results/
.logs

data.zip