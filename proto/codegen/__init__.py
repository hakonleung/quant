"""Hand-written codegen for cross-process contracts (CLAUDE.md §8).

Reads source schemas under ``proto/`` and emits language-specific modules.
Run via ``python -m proto.codegen`` (or ``pnpm gen:proto``).
"""
