# 16 — TA (Technical Analysis, beta)

> Pure price/volume analysis. Lives next to `analyze` (sentiment) but is
> **its own top-level surface** — the terminal command and the BFF route
> family use the `ta` namespace, not `analyze ta`.

## 1. Surfaces

| Surface  | Endpoint / command                        | Purpose                                          |
| -------- | ----------------------------------------- | ------------------------------------------------ |
| Terminal | `ta`, `ta <code>`, `ta sector <id\|name>` | Single-stock TA + sector fan-out                 |
| Web BFF  | `POST /api/ta/analyze_one`                | Single-stock TA (LLM, paid)                      |
| Web BFF  | `GET  /api/ta/analyze_one?code=`          | Cached single-stock TA (cache miss → 404)        |
| Web BFF  | `POST /api/ta/analyze_many`               | Sector fan-out + LLM summary                     |
| Action   | `analyze.ta`                              | Single-stock; cacheKey `analyze.ta:<code>`       |
| Action   | `analyze.ta.many`                         | Sector; cacheKey `analyze.ta.many:<sorted-codes>` |

## 2. Pipeline

The full TA pipeline runs in NestJS (no Python LLM):

- `apps/api/src/modules/ta/ta.service.ts` — single-stock orchestrator.
  Reads meta (`StockMetaService`) + last 90 daily bars (Flight
  `list_kline_for_code` returns qfq prices already), builds the prompt
  and calls `LlmService.completeJson(scope='ta')`, decodes JSON →
  `TaAnalysis`, writes the result through `TaCacheStore`.
- `apps/api/src/modules/ta/ta-cache.store.ts` — file-per-code JSON cache
  at `data/ta/{code}.json`; `(code, asof)` key (`asof` follows the
  latest bar's date so the cache invalidates with the data, not the
  wall clock). Replaces the deleted Python `ParquetTaCache`.
- `apps/api/src/modules/ta/prompts/ta-analyze.prompt.ts` — verbatim
  port of the Chinese system + user prompts.
- `apps/api/src/modules/ta/domain/decode-ta.ts` — pure JSON →
  `TaAnalysis` decoder; tolerant on bad sub-rows, throws `LLM_FAILED`
  on missing trend.

`POST /api/ta/analyze_many` is **NestJS-side fan-out**:

1. For each `code` in body, call `TaService.analyzeOne` in parallel.
   Each per-stock call hits `TaCacheStore` first, so a sector whose
   members were analysed recently is mostly cache reads.
2. Reduce to a `TaSectorAnalysis`:
   - `trendBreakdown`: `{up,down,sideways}` member counts
   - `overallDirection`: argmax over the breakdown
   - `overallConfidence`: average confidence of members in the
     overall direction
   - `members[]`: slim `TaSectorMember` per stock (drops level lists
     and patterns; keeps the leading R/S prices)
3. Build a sector narrative via `LlmService.completeJson` (scope `ta`).
   Prompt is `apps/api/src/modules/ta/prompts/sector-summary.prompt.ts`.
   On LLM failure, `summary` is empty — caller still gets the numerical
   aggregate, with caveats describing each member that errored.

Python end no longer carries any TA code: `ta_service.py` /
`ta_prompts.py` / `parquet_ta_cache.py` / `quant_rpc/ops/ta.py` are
all deleted. Only `list_kline_for_code` (a pure parquet read) remains
on the Python side.

## 3. Schemas

`packages/shared/src/types/ta.ts`:

- `TaAnalysisSchema` — single-stock view (unchanged).
- `TaSectorMemberSchema` — per-stock card embedded in the aggregate.
- `TaSectorAnalysisSchema` — aggregate response: `codes`,
  `trendBreakdown`, `overallDirection`, `overallConfidence`, `members[]`,
  `summary`, `caveats`, `cachedAt`.

## 4. Tests

- `apps/api/test/modules/ta/ta.controller.spec.ts` — unit covers GET
  cached, POST analyze_one, POST analyze_many (golden + all-fail path).
- `packages/terminal/src/commands/ta.test.ts` — terminal command
  surface: subcommand list, single-stock dispatch, `ta sector`
  resolves the sector then calls `analyze.ta.many`, missing arg /
  invalid code branches.
